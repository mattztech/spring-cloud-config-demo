spring:
  application:
    name: client-config-demo
  cloud:
    config:
      uri: http://localhost:8888

---

spring:
  profiles:
    active: docker
  cloud:
    config:
      uri: http://server:8888
